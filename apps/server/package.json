{
  "name": "server",
  "main": "src/index.ts",
  "type": "module",
  "scripts": {
    "build": "tsdown",
    "check-types": "tsc -b",
    "compile": "bun build --compile --minify --sourcemap --bytecode ./src/index.ts --outfile server",
    "dev": "bun run --hot src/index.ts",
    "start": "bun run dist/index.js",
    "test": "bun test"
  },
  "dependencies": {
    "@task-tracker-app/auth": "workspace:*",
    "@task-tracker-app/db": "workspace:*",
    "@task-tracker-app/logger": "workspace:*",
    "better-auth": "catalog:",
    "cors": "^2.8.5",
    "dotenv": "catalog:",
    "express": "^5.1.0",
    "http-errors": "^2.0.1",
    "uuid": "^13.0.0",
    "zod": "^4.1.11"
  },
  "devDependencies": {
    "@task-tracker-app/config": "workspace:*",
    "@types/bun": "^1.2.6",
    "@types/cors": "^2.8.17",
    "@types/express": "^5.0.1",
    "@types/http-errors": "^2.0.5",
    "@types/supertest": "^6.0.3",
    "supertest": "^7.1.4",
    "tsdown": "^0.16.5",
    "typescript": "^5.8.2"
  }
}
